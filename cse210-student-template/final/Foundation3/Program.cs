using System;

class Program
{
    static void Main(string[] args)
    {
        Console.WriteLine("Inheritance with Event Planning");

        //we set the address and other details here

        // Create Address objects: Create at least one or two Address instances.

        // Create Event instances: Create one instance of each (a Lecture, a Reception, and an Outdoor event).

        // The Test Loop: Create a List<Event> and add all three events to it. 
        // Then, use a foreach loop to iterate through the list and call StandardDetails(), FullDetails(), and ShortDesc() for each.
    }
}